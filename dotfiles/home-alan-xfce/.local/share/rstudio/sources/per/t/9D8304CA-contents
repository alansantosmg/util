

# Matriz para entendimento da situação das linhas de montagem
# de uma fábricca em um dia. 


# Linha de montagem 1 (mais lenta)
# Produziu 6 componentes sem defeito e 2 com defeito = 8 componentes

# Linha de montagem 2 (mais rápida)
# Produziu 9 componentes sem defeito e 1 com defeito = 10 componentes

# Qual probabilidade de escolher 1 componente para demonstração montado
# na linha 1? 

# Qual probabilidade de escolher um componente da linha 1 tendo escolhido
# um componente com defeito? 


# Cria matrix de 2x2
montagem <- matrix(1:4, nrow =2)
montagem

# atribui componentes defeituosos na primeira coluna (c_def)
# atribui componentes normais na segunda coluna (s_def)
# cada linha da matriz será uma linha de montagem (lm)
montagem[1,1] <- 2
montagem[1,2] <- 6
montagem[2,1] <- 1
montagem[2,2] <- 9
montagem
colnames(montagem) <- c("c_def","s_def")
rownames(montagem) <- c("lm_1", "lm_2")
montagem

# O total de componentes é 18 logo: 
# P(escolha componente lm_1) = é 8/18
# P(escolha componente lm_1 | escolha componente defeituoso)

# Dada a escolha de um componente defeituoso, 
# qual a probabilidade desse componente ser da linha 1? 


